<policies>
    <inbound>
        <base />
        <!-- Team Gamma: strict rate limit — R&D and experimentation -->
        <!-- Replace with llm-token-limit after importing Foundry API via portal -->
        <rate-limit-by-key calls="50" renewal-period="60"
            counter-key="@(context.Subscription.Id)" />
    </inbound>
    <backend>
        <base />
    </backend>
    <outbound>
        <base />
    </outbound>
    <on-error>
        <base />
    </on-error>
</policies>
